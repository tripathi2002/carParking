{
  "name": "scope",
  "version": "1.0.0",
  "description": "Two-way data binding,Best practices using the scope, The $rootScope object, Broadcasting the scope",
  "main": "index.js",
  "scripts": {
    "start": "npx nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vibhu Tripathi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
